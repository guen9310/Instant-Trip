// 코스 생성·재추천·주변정보 조회 등 서버 액션 호출에 공통으로 씌우는 클라이언트 측
// 타임아웃(withTimeout과 함께 사용). 파이프라인 내부 외부 API 호출들은 각각
// 10초 타임아웃을 두지만(server/weather.ts, lib/clients/*), 그 여러 호출이 순차·
// 병렬로 얽혀 있어 정상적으로도 수 초~10여 초가 걸릴 수 있다 — 정상 응답을 오탐으로
// 끊지 않도록 여유 있게 20초로 잡는다.
export const COURSE_ACTION_TIMEOUT_MS = 20_000;
